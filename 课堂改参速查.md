# 课堂改参速查

**只打开 `plate_default_parameters.m`，修改“课堂改参区”标记下对应的一行。** 保存文件后，在 MATLAB 命令窗口输入 `run_assignment`。不用改有限元核心代码。

| 老师要求 | 改成这样 | 含义 |
|---|---|---|
| 载荷 2000 N/m² | `cfg.q = 2000.0;` | 均布载荷沿 +z |
| 比较 4、8、16 网格 | `cfg.mesh_sizes = [4, 8, 16];` | 依次计算 4×4、8×8、16×16 |
| 改为右边固支 | `cfg.bc = 'x1_clamped';` | `x=Lx` 固支，其余三边自由 |
| 改为下边固支 | `cfg.bc = 'y0_clamped';` | `y=0` 固支，其余三边自由 |
| 两边固支 | `cfg.bc = 'x0_y1_clamped';` | `x=0` 和 `y=Ly` 固支 |
| 四边固支 | `cfg.bc = 'all_edges_clamped';` | 四条边都固支 |

单边名称还可用 `'x0_clamped'`（左）、`'y1_clamped'`（上）。`x1` 指 `x=Lx`，`y1` 指 `y=Ly`。多个边名可用下划线连接，例如 `'x0_x1_clamped'`；未列出的边自由。当前边界选项均为**固支**，没有简支选项。

运行后，MATLAB 命令窗口会显示各网格峰值、峰值位置和反力，三张图窗会展示最新算例；再次运行会替换旧图窗。也可以打开 `results/summary.txt` 和 `results/mesh_convergence.csv` 查看完整数据，三张 PNG 会同步重生成。网格列表按从粗到细填写，最细一项是当前结果的数值参考。

**注意：** 已有的计算报告 PDF、课堂汇报 PPTX 和提交 ZIP 记录的是默认算例，不会因改参或重新运行 MATLAB 自动更新。现场展示改参结果时，使用刚生成的 `results` 文件，不要把默认报告中的数值说成新算例结果。
